<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.oz.ozHouse.merchant.repository.mapper.MerchantMapper"> 
	
	<select id="mybatisMerId" parameterType="int" resultType="String">
		select m.merId from ozHouse.Merchant m where m.merNum = #{merNum}
	</select>
</mapper>